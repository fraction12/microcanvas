# microcanvas

A lightweight, reliable canvas tool for AI coding tools.

Microcanvas is a standalone canvas runtime and viewer that any AI coding tool can use. It should do the same core job as OpenClaw Canvas, but in a smaller, more predictable, tool-agnostic way. Agents call Microcanvas like any other tool and Microcanvas owns the rendering and viewing experience.
